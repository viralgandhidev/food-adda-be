-- Seed: 10 users, and for each user 5 products with random categories
-- NOTE: This migration requires stored procedures (WHILE loops, IF statements)
-- Since MySQL migration scripts can't easily use stored procedures, this migration
-- is disabled. Use migration 023_seed_users_and_products_simple.sql instead.
-- This file is kept for reference but does nothing (idempotent - always skips).

-- This migration is intentionally empty/non-functional because it requires
-- stored procedure syntax (WHILE loops, IF...THEN) which can't be used in
-- migration scripts. The simpler migration 023 should be used instead.
